execute ! RUN GAME to run the game (obviously).
if you run RUN GAME and python is not installed it will try to download python and close. run it again to see if it installed
requirements:
  python, 
  python libraries:
    PIL (pillow)
    json
    pygame

you should be already playing the game by running the main file (RUN GAME) 2 times.
first time running may take a while to open
